# Graph links are before-after only; the Owner accepts them; the Plan rewrites on request

A Link means “learn this Point before that Point”. Related-but-unordered ties are out of the first release. An Agent may propose Links; they enter the Graph only after the Owner accepts. The Brain rewrites the Plan only when the Owner asks, and tries to keep Owner edits. This keeps the Graph simple enough to sort a Plan.
